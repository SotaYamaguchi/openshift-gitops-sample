apiVersion: rhdh.redhat.com/v1alpha3
kind: Backstage
metadata:
  name: developer-hub
  namespace: rhdh
spec:
  application:
    replicas: 1
    route:
      enabled: true
  database:
    enableLocalDb: true
